---
# vars file for docker
docker_req_pack:
  - apt-transport-https
  - ca-certificates
  - curl

docker_pack:
  - docker-ce
  - docker-ce-cli
  - containerd.io
  - docker-buildx-plugin
  - docker-compose-plugin
